#include "shell.h"

int main(void)
{
    int age;
    char name[20];

    printf("Enter your name: ");
    fgets(name, sizeof(name), stdin);

    printf("Enter your age: ");
    scanf("%d", &age);

    printf("Hello, %s", name);
    printf("You are %d years old.\n", age);

    return 0;
}
